options gen2
require daslib/fio

[export]
def initialize(project_path : string) {
    if (das_is_dll_build()) {
        register_dynamic_module("{project_path}/dasModuleClipboard.shared_module", "Module_Clipboard")
    }
    register_native_path("clipboard", "clipboard", "{project_path}/daslib/clipboard.das")
}
