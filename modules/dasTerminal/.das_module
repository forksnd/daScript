options gen2
require daslib/fio

[export]
def initialize(project_path : string) {
    if (das_is_dll_build()) {
        register_dynamic_module("{project_path}/dasModuleTerminal.shared_module", "Module_Terminal")
    }
    register_native_path("terminal", "terminal", "{project_path}/daslib/terminal.das")
}
